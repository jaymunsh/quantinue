# 자주 나올 질문 — Q&A 정본 (2026-07-29 발표)

> **Q&A의 유일한 정본이다** — 백업 슬라이드는 07-28에 덱에서 뺐다(문서로
> 일원화). 발표 전에 종이로 뽑아 대본과 함께 두고, 질문이 오면 화면은
> Q&A 장에 둔 채 말로 답한다.
> 되받아치기(💥)와 말조심(⚠️)은 `대본.md`의 해당 화면 절에도 있다.

## 1. 범위·설계

**실거래는 왜 안 하나요?**
브로커 주문만 뺐고 나머지는 전부 실물입니다. 켜는 조건은 **무인 검증 통과 + 실거래 요건 검토**입니다. 기술만의 문제가 아니라 — 본인 계좌 API 자동매매는 브로커 실계좌 약관 범위지만, **타인 자금을 운용하거나 자문하는 순간 인가가 필요한 영역**이라 그건 범위 밖으로 명시했습니다. (⚠️ "법률 자문을 받았다"고 말하지 않는다 — 켜기 전 검토 목록으로 관리한다까지만.)

**배포는 왜 안 했나요?**
**판단이 뒤집힌 항목입니다.** 처음엔 "필요 없다" → 운용자 이동을 겪고 **"이전이 정당하다"**로 재판정, 착수만 보류했습니다.

**AI가 시장을 이기나요?**
수익률이 아니라 파이프라인과 비용 통제가 가치입니다. `/me`에 SPY 비교 화면이 있습니다.

**영상은 각본 아닌가요?**
맞습니다. 그래서 마지막 16초에 **실제 운영 기록**을 붙였습니다. 그 앞에는 데모 원장을 psql로 직접 조회한 화면도 있습니다 — 주문 34 = 체결 34, 중복 주문키 0, 중복 체결ID 0.

**계좌가 왜 여러 개?**
운용 6 + 회귀 검증용 4 = **등록 10**입니다. 관제실이 **9**로 세는 건 1개가 일시정지라 활성만 세기 때문입니다. 시연 화면에서는 검증용을 뺐습니다.
(성과 곡선의 **5계좌 합산**은 일시정지 1계좌를 제외한 활성 운용 계좌 기준입니다.)

**계좌 이름이 왜 DEMO-?**
브로커 **모의 계좌**라는 뜻입니다. 실제 운영인지는 좌하단이 말해 줍니다 — 운영 `LLM openai`, 데모 `LLM mock`.

**지금 다 켜져 있나요?**
1분 방어와 텔레그램 알림은 **운영에서 켜져 있습니다**. 장중 AI 재판단·웹소켓 스트림은 완성됐지만 **검증을 통과한 뒤 켭니다** — 영상의 재판단은 데모 환경입니다.

**비평가가 반박하면 전략가가 다시 반론하나요?**
아니요, **1회전**입니다. 반박에 걸린 제안은 폐기되고 반박 사유만 원장에 남습니다. 왕복 토론은 "개선인지 설득당함인지"를 성과 데이터 없이 구분할 수 없어 뒤로 뺐고, 대신 **과신할수록 통과 문턱이 올라가는** 비대칭 게이트를 넣었습니다. 판단·반박이 전부 원장에 쌓이고 있으니, 성과 검증이 쌓이면 그때 왕복을 붙이는 게 순서라고 봤습니다.

**왜 상위 20종목만 AI가 보나요? (원래 50 아니었나)**
둘 다 살아 있습니다. 지표 점수는 원장의 봉으로 계산돼 공짜라 **상위 50까지 기록**하고, **AI 판단은 상위 20만** 보냅니다. 최대 보유가 15종목(공격 10 + 안전 5)이라 후보 20이면 충분하고, 하위권은 어차피 확신 문턱(0.65/0.75)을 못 넘어 넓혀도 늘어나는 건 비용뿐입니다. 20은 코드가 아니라 정책 파일(pipeline.yaml)의 숫자 하나 — 검증되면 넓히면 됩니다.

**앱을 실수로 두 개 켜면요?**
두 번째 프로세스는 먼저 뜬 쪽이 걸어둔 잠금(PID 파일)을 보고 **서버가 뜨기 전에 스스로 종료**합니다. 혹시 잠금이 뚫려도 주문 고유 키(`idempotency_key` UNIQUE)가 DB에서 두 번째 주문을 거부합니다 — **두 겹**입니다.

**본인은 뭘 했나요?** (발표자 김지현 기준)
PM으로 1·2차 전체의 범위와 우선순위를 정하고, 단계마다 산출물을 검수했습니다. 구현 세부는 담당 파트 기준으로 답변드립니다.

> 원장: `tb_account` 운용 6 + TEST-* 4 · 주문 62 = 운용 27 + 검증용 35

## 2. 숫자·검증

**환각은 어떻게 막나요?**
근거 묶음 제공 → 비평가 반려(검토 323건 중 57건, 18%) → 판단 기록으로 사후 추적합니다. "막는다"가 아니라 **"거르고 추적한다"**입니다.

**반려 57건은 누가 걸렀나요?** (11화면 관련)
**AI 비평가의 반박 52 + 기계 게이트 차단 5**입니다. 게이트는 명백한 결격 — 데이터 이상(고가<저가, 하루 30% 초과 스냅샷)·위험회피 국면의 신규 매수 — 을 AI 없이 자르고, 통과한 제안은 비평가 AI가 전수 반박합니다(실측 호출 185회). ⚠️ 옛 문구 "84%는 AI까지 안 간다"는 07-29 원장 검증에서 사실과 달라 폐기됨 — 쓰지 말 것.

**신뢰도 점수는 AI가 매기나요?** (9화면 맥락 채점)
아니요, **정책표 조회**입니다. 뉴스는 매체 도메인 등급표(로이터·블룸버그 등 allow **0.95** / 세미 매체 gray **0.50** / 레딧·X block **0** — 목록에 없는 매체는 gray), 내부자 거래(Form 4)는 **자발적 공개시장 매수 0.80 · 매도 0.30**만 신호로 치고, 보상·계획매매(10b5-1)는 무시, 재량 거래가 없으면 점수 자체를 안 냅니다(기권). 바꾸려면 코드가 아니라 정책 파일을 고칩니다.

**채점에 왜 AI를 안 쓰나요? (기획 때는 있지 않았나)**
초기 설계엔 공시·뉴스 채점에도 LLM 자리가 있었습니다(코드에 태스크 예약 흔적이 남아 있음). 실측해보니 **사실만 있는 자리에서 모델은 0.5 중립으로 도망쳤고**, 그 상수가 판단을 오염시켰습니다. 그래서 채점은 정책표로 바꾸고, AI는 공식이 없는 종합 판단 두 자리(전략가·비평가)에만 남겼습니다 — "쓸 곳과 안 쓸 곳"의 실측 사례입니다.

**재현 기록이 234건뿐인가요?**
07-21 이후 판단은 **240 = LLM 234(누락 0) + 규칙 6**입니다. 그 6건은 손절·익절 자동 청산이라 **애초에 프롬프트가 없습니다** — "손절은 AI에게 안 묻는다"의 원장 증거입니다.

**계보를 전부 남기나요?**
모델·프롬프트 버전·입력 지문은 남깁니다. **`policy_version`은 컬럼만 잡아두고 배선은 안 했습니다** — 342건 전부 비어 있습니다.

**공시·뉴스는 판단에 쓰이나요?**
수집·정규화까지는 돕니다(뉴스 10,728 · 공시 8,973). 다만 **채점을 판단에 물리는 구간은 아직 얕습니다** — 계보 기준 6/342 · 0/342. **모은 것과 판단에 쓴 것은 다릅니다.**

**프롬프트 관리는?**
프롬프트 버전을 판단 원장에 함께 기록합니다(07-21 이후 234건). 성향별로 분리돼 있습니다.

**AI 비용이 얼마나?**
실측 4일 **$0.53**(하루 ~$0.13), 하루 상한 $3입니다 — 상한의 6% 수준. 요율을 선언하지 않은 모델은 **기동 자체를 거부합니다**.

**영상의 VRDN 손절선은 −15%가 아니던데요? (139.50 = −7%)**
맞습니다 — **데모 각본이 2분 안에 발동 장면을 보여주려고 심은 값**입니다(각본 급락 폭이 그 선을 밟도록). 같은 화면의 다른 보유(AAPL 283.09 등)는 실규칙 **−15%/+20% 그대로**이고, "살 때 각인 → 1분 감시 → 자동 청산 → 원장 기록" 경로 자체는 실코드입니다. 참고로 손절·익절선은 두 성향 공통값입니다(pipeline.yaml 전역 키).

**영상 중간의 psql 화면은 뭔가요?**
데모 원장 무결 검증입니다. `demo_preflight.sh`를 돌린 뒤 `tb_order`·`tb_fill`을 직접 세고, `idempotency_key`와 `broker_fill_id`를 `GROUP BY … HAVING count(*)>1`로 잡아 **중복이 0인지** 확인한 출력입니다. 편집 없는 실제 실행 화면이고, 주황 테두리 안 한 줄이 결론입니다.

**T+5 회고는요?**
1차에서 설계·구현한 파트. 첫 T+5 대상이 **07-27에야 도래**했고(현재 대기 대상 = 07-20·21 체결 46건), 회고 실행은 아직 일일 잡이 아니라 수동 트리거라 **2차 원장 0건**(`tb_review`)입니다 — 실적은 주장하지 않습니다. AI 논평 자리(REVIEW 태스크·prompt_version 컬럼)는 예약만 해뒀습니다. **기획상 회고 결과의 판단 반영 가중치는 표본이 충분히 쌓일 때까지 0에 가깝게 둡니다** — 소표본 회고로 전략을 흔들지 않기 위해서입니다.

**주문=체결이 중복 없음의 증거인가요?** (14화면 관련 · 대본에도 있음)
그 등식은 미체결·거절이 없었다는 뜻입니다. **중복 방지는 따로 보장됩니다** — 계좌+시그널로 파생한 주문 고유 키(`core/order_identity.py`)에 `tb_order.idempotency_key UNIQUE` 제약이 걸려 있어, 재시작이 겹쳐도 두 번째 시도는 키가 막습니다.

**그래서 벌었나요?** (15화면 관련)
7거래일로는 실력과 우연을 구분할 수 없습니다. 구분할 수 있을 만큼 쌓는 것이 다음 단계입니다.
(SPY는 07-27 종가까지 6거래일 적재라 "같은 기간 비교"라고 말하지 않는다.)

**성과를 높일 계획은 있나요?** (손실 추궁 포함 — 되받아치기 정본)

먼저 프레임 — **−0.34%는 같은 기간 시장(−0.44%)보다 덜 빠진 것**이라 "손해가 크다"고 인정하고 들어가지 않는다. "7거래일은 실력·우연을 구분하기엔 짧고, 개선은 감이 아니라 원장 데이터로 한다"가 기조. 개선 후보는 네 갈래:

**① 규칙 튜닝 — 이미 쌓인 데이터로 바로 가능한 것**
- 손절·익절을 일괄 −15%/+20%에서 **변동성(ATR) 기반**으로 — 조용한 종목은 타이트하게, 거친 종목은 여유 있게
- 확신 문턱(0.65/0.75)을 판단 342건의 **문턱별 적중률로 역산** — 지금 값은 데이터 없이 정한 초기값
- **판단 지평 5일 vs 시간 청산 10일 정합** — 5일 안에 +20%는 드물어 익절보다 시간 청산이 먼저 오는 경향을 인지하고 있음
- 손절선의 **성향별 분리 실험** — 지금은 두 성향 공통, 정책 파일 키 하나라 실험 비용이 낮다

**② 회고 루프 가동 — 정공법**
- T+5 회고(대기 46건)를 돌려 "어떤 판단이 실제로 맞았나"를 **프롬프트·문턱에 되먹임**
- 단 반영 가중치는 **표본이 쌓일 때까지 0 근처**(기획) — 소표본 회고로 전략을 흔들지 않는다
- AI 회고 논평 자리(REVIEW 태스크)는 설계에 예약돼 있음

**③ 판단 입력 강화 — 모아둔 것을 판단에 잇기**
- **채점→판단 배선** — 이미 수집한 뉴스 10,728·공시 8,973의 신뢰도 점수가 판단에 얕게만 닿아 있다(6/342·0/342). 배선만 이어도 근거 폭이 넓어진다
- **자료 폭 확장** — 실적 캘린더·수급 등 현업이 참고하는 자료 추가 (18화면 "다시 한다면"의 교훈 그대로)

**④ 구조·집행 실험 — 이 시스템이라서 가능한 것**
- **리플레이 백테스트** — 입력 지문·프롬프트 버전이 원장에 있어 과거 7거래일을 그대로 재생하며 규칙만 바꿔 실험 가능 ("손절 −12%였다면?"을 재실행으로 답한다)
- **프롬프트 A/B** — prompt_version별 적중률 비교로 프롬프트 수정을 성적표로 채택
- **두 성향 합의 필터** — 독립 판단은 유지하되, 둘 다 매수인 종목만 사는 교집합 전략의 정밀도 측정
- **국면별 노출 조절** — 위험회피 국면에서 주식 비중 자체를 축소 (지금은 신규 매수 차단까지만)
- **섹터 상한** — 지금은 종목 한도뿐이라 추세 상위가 한 섹터에 몰리면 동일 방향 리스크 중복
- **실적 발표 블랙아웃** — 발표 직전 신규 진입 금지 (지표가 무력해지는 구간의 실무 관행)
- **집행 개선** — 분할 진입·트레일링 스탑(이익 따라 손절선 인상) 등 판단 외 비용 절감

닫는 말: **"전부 실험 가능한 상태입니다 — 판단·근거·결과가 원장에 남아 있어서, 감이 아니라 데이터로 튜닝하는 것이 이 시스템의 성과 개선 방식입니다."** (①→④ 순서가 곧 착수 순서 — 가까운 것부터)

> 원장: `prompt_version` 234/342 · `policy_version` 0/342 · `src_news_at` 0/342 · `tb_review` 0
> **덱 수치는 전부 07-19 → 07-27 종가 구간(운영 7거래일) 기준.** 07-29 아침 실측은 판단 396 · 체결 68 · AI 비용 5일 $0.71 —
> "그 뒤로도 돌았나요?"가 오면 이 숫자로 답한다(시스템이 발표 준비 중에도 계속 돌았다는 증거).

## 3. 참고 — 시간 한눈에 (질문 대비)

한 줄 요약: **"낮 1시에 판단하고, 밤 10시 반부터 지키고, 닷새 뒤 되돌아본다."**

| 시간 | 무엇 |
|---|---|
| 60초 | 잡 러너 틱(할 일 탐색) · 장중 방어 감시 주기 |
| 5분 | Healthchecks 생존 신호 (시스템 밖 감시자) |
| 10분 / 15분 | 보도자료 / 뉴스 수집 주기 |
| 30분 | 같은 종목 AI 재호출 금지 |
| 13:00 KST | 뉴욕 자정 = 새 거래일 — 잡 14종 시작(수집→선별→판단→주문 준비) |
| ~13:05 | 텔레그램 일일 요약 |
| 22:30–05:00 KST | 미국 정규장(서머타임 기준 · 겨울은 23:30–06:00) — 체결과 1분 방어 |
| ±5% 급변 | (시간 아님) 장중 AI 재판단 트리거 — 뉴스·공시도 동일 |
| 5영업일 | 전략가 판단 지평 · T+5 회고 시점 |
| 10거래일 | 시간 청산 (손절 −15% · 익절 +20%와 함께 방어선 3종) |

13:00에 왜 판단하냐고 물으면: **뉴욕 날짜가 바뀌어 전날 일봉·공시가 전부 확정된 가장 이른 시각**이라, 장이 닫혀 있는 낮에 판단을 끝내고 밤 개장 때는 집행·감시만 합니다. 증권사 앱의 프리마켓(17:00~22:30)·애프터마켓은 우리가 안 쓰는 확장 세션 — **정규장만 감시·거래**합니다.

## 4. 참고 — 역할 11개 vs 잡 14개 (헷갈릴 때)

**역할 = 책임의 단위(코드 조직도, "누가"), 잡 = 실행의 단위(하루 시간표, "언제 무엇을")** — 부서 11개와 일과표 14칸의 관계라 숫자가 다르다. 1:1이 아닌 지점 넷: 수집은 역할 하나가 잡 여럿으로 쪼개지고(따로 실패·재시도), 전략가+비평가는 한 잡에서 연달아 돌고(한 흐름), 회고는 잡이 없고(수동), daily_summary는 역할이 없다(알림 인프라).

| 역할 (role_01~11) | 한 줄 |
|---|---|
| 01 유니버스 스크리너 | 시총 상위 2,000 상장사 목록 관리 |
| 02 기술 분석 | 봉에서 지표(이평·RSI·수익률) 계산 |
| 03 일일 스크리너 | 지표 점수로 상위 20 + 보유 선정 |
| 04 거시 분석 | 금리로 시장 국면 판정 |
| 05 공시 분석 | SEC 공시 정규화 + Form 4 채점 |
| 06 뉴스 분석 | 뉴스 정규화 + 매체 신뢰도 점수 |
| 07 전략가 (AI) | 종목마다 매수/매도/보류 + 확신도 |
| 08 비평가 (AI) | 게이트로 결격 자르고, 통과분 전수 반박 |
| 09 리스크·포트폴리오 | 비중·한도·현금 바닥 계산 |
| 10 주문 집행 | (모의)주문 집행 — 손절·익절 각인, 중복 키 차단 |
| 11 회고 | T+5 재평가 (현재 수동 · 잡 없음) |

| 잡 (등록 순서 = 의존 순서) | 한 줄 | 역할 |
|---|---|---|
| universe | 상장사 목록 갱신 | 01 |
| daily_bars | 전 종목 일봉 수집 | 01·02 |
| benchmark_spy | SPY 기준선 수집 | 01 |
| disclosures | SEC 공시 수집·정규화 | 05 |
| news | 뉴스 수집·정규화 | 06 |
| news_wire | 보도자료 RSS 수집 | 06 |
| macro | 시장 국면 판정 | 04 |
| screening | 후보 20종목 선정 (API 0콜) | 02·03 |
| insider_scoring | Form 4 내부자 거래 채점 | 05 |
| analysis:aggressive | 공격형 판단 + 비평가 반박 (AI) | 07+08 |
| analysis:conservative | 안전형 판단 + 비평가 반박 (AI) | 07+08 |
| exits | 손절·익절·시간·매도판단 청산 | 09·10 |
| allocation | 승인 후보 매수 (한도 내) | 09·10 |
| daily_summary | 텔레그램 일일 요약 | (알림) |

기준 하나만 기억: **"누가 책임지나?" → 역할, "오늘 언제 도나?" → 잡.**

