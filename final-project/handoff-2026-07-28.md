# 세션 핸드오프 — 2026-07-28 (발표 D-1)

> 오늘(07-28) 낮 세션에서 한 일의 전체 기록이다. 커밋 25개(이 갱신 포함), 전부
> `origin/main`에 **푸시 완료**.
> 남은 일은 `NEXT.md`, 발표 인수인계는 `to_jihyun.md`, 대본은
> `slides/대본.md`가 정본이다. 이 문서는 "무엇이 왜 바뀌었나"의 스냅샷.

---

## 한 줄 요약

**발표 자료가 완성됐다.** 덱은 사용자와 3라운드 전수 검토를 거쳐
본편 14 → **17장**으로 재구성됐고, 영상은 **2:14 재편집본**(저장소 포함),
캡처 2장 반입, 대본은 덱에서 분리해 md로. 그 와중에 **운영 사건 둘**
(모니터링 적중, allocation 버그)을 겪었고 둘 다 발표의 재료가 됐다.

## 1. 덱 — 본편 17장 + 백업 2장 (`slides/index.html`)

### 구조 변화

- **삭제**: "지금 상태(구현≠켜짐)" 장 — 내용 중복. 문답은 백업 1로 이동
- **신설 4장**: 시스템 아키텍처(6) · 하루 파이프라인(7) — 다이어그램형,
  **투자 페르소나(9)** — 성향별 방법론(오닐·미너비니 / 하워드 막스) +
  **pipeline.yaml 세팅 비교표**(매수 0.65/0.75 · 매도 0.60/0.50 ·
  비중 20/10% · 현금 10/30% · 일손실 4/2%) + 실제 운용(판단 175/167건 ·
  계좌 3/2) + "지어내지 마라" 규칙 — 사용자 요청 3회에 걸쳐 완성,
  **결과 곡선(14)** — 운용 vs SPY 지수 비교(아래 §5)
- **모니터링 장(11)**: "감시자는 누가 감시하나" → "모니터링 3층" 프레임,
  발표 전날 실제 사건 타임라인 + 캡처 2장
- 표지 팀 라인·메타 삭제, "MVP-2" 표기 전면 제거(사용자: "이건 최종"),
  마무리 장은 선언문만 가운데 정렬

### 전수 검토에서 잡은 것 (사용자 3라운드)

- 어투 전면 통일: 화면 문구 합니다체 · 제목 짧은 다체 · 각주는 데이터
- 원장 각주(footer)를 **전 장 같은 좌표(833px)로 계측·고정** — 원인은
  `.finale{position:relative}`가 슬라이드를 내용 높이로 쪼그라뜨린 것과
  `.shot-todo b{display:block}`이 굵은 구절마다 줄을 가르던 것
- `.kpi div` 선택자 버그(숫자 옆 유령 세로줄), 깔때기 정렬(숫자를 막대 앞
  고정 칸 + 트랙 배경), 페이지 번호를 QUANTINUE 오른끝선에 정렬
- **무대 중앙 정렬을 grid 의존 → `left/top 50% + translate`로 교체** —
  다른 PC(창<1600px)에서 무대가 구석으로 밀리던 문제의 근본 수정.
  전체화면(F)은 여백 0으로 꽉 채움
- **발표자 창(P)·노트(N) 기능 제거** — 대본을 보면서 발표하지 않기로 함.
  덱은 `F`+이동만, 대본은 `slides/대본.md`(영상 내레이션 8줄 포함)

### 사실 정정 (app-v2 실측 조사로 확인)

- 하루 후보 = **상위 20 + 보유 종목** (`llm_depth: 20`). 222는 하루치가
  아니라 **7슬롯일 누적**. `daily_picks: 50`은 코드 소비자 없는 죽은 설정
- 1차 MVP = **공격형 1종 · 계좌 1개 · 수동 1사이클 · 매수 전용** 기능 검증
  (`docs/weekly-report-fist-week.md`) — "하루 한 번만 판단했다" 프레임 폐기
- 데모 포트는 8022 (8021은 개발용)

## 2. 영상 — 2:55 → **2:14**, 저장소 포함

- 데모 구간 상단 배지 삭제(마지막 "실제 운영 기록" 배지는 전환 장치라 유지),
  막별 차등 배속(핵심 막 1.2×, 나머지 1.35~1.5×), 자막은 하단(증거를 덮는
  VRDN 컷 하나만 상단). 포스터도 새 컷에서 재추출
- **`footage/summary-3min.mp4`(12MB)를 gitignore 예외로 저장소에 포함** —
  클론만 받으면 재생된다. 원본 2:55는 `footage/archive-old-takes/`
- 재빌드: `node shoot/build_rough.mjs && node shoot/build_summary.mjs`
  (`build_summary`의 `-t`는 입력 옵션 위치여야 한다 — 오늘 잡은 함정)

## 3. 캡처 2장 — 완료 (`slides/assets/`)

- `healthchecks.png`: 사용자 원본에서 **ping UUID URL·이메일 2곳 가림** 후,
  상태·가동률 + 빨간 박스(09:13 down · 09:36 up)를 슬라이드 비율로 합성
- `telegram-alerts.png`: 07-28 스레드(기동→장애→복구→방어선→실패→요약)를
  가로 2열 합성 — "실패까지 그대로 도착한다"의 증거

## 4. 운영 사건 둘 (발표 재료)

1. **아침 — 모니터링 적중**: 이동 중 절전 → 09:13 Healthchecks 장애 판정
   (메일+텔레그램) → 09:36 복구. 덱 10화면 사건 스트립이 이것. 15초 문안
   은 대본에
2. **오후 — allocation 버그, 당일 수정 배포**: Alpaca 시세 579.865(센트
   미만)가 돈 계약(소수 2자리)에 걸려 13:05부터 1분마다 실패+알림 반복 →
   진입가 `round(,2)` 한 줄 + 회귀 테스트(`b7110e0`) → **유닛·웹 804** ·
   Ruff clean → 13:16 재기동(PID 64727) → 13:16:59 정상 실행, 이후 실패 0.
   fail-closed(잘못된 주문 없음)였다는 점이 Q&A 답. 알림 중복 억제 부재는
   발표 후 개선 항목

## 5. 결과 곡선 (14화면) — 원장 실측

`tb_account_equity_daily` 07-20→07-28, 운용 5계좌 합산 vs SPY 지수화:
**운용 −0.34% vs SPY −0.44%** (공격형 3계좌 −0.55~−0.65% · 안전형 2계좌
+0.02~+0.19%). 프레임: "주장하기엔 짧지만 숨기지 않는다 — 곡선이 매일
원장에 남는다". 차트 팔레트는 CVD 검증 통과(보라/초록).

## 6. 문서 — 오늘 만들거나 크게 고친 것

| 파일 | 무엇 |
|---|---|
| `to_jihyun.md` | 발표 인수인계 — 반나절 이해 코스 + 당일 체크리스트 |
| `slides/대본.md` | **대본 정본** (17장 + 백업 + 영상 내레이션) |
| `slides/모드-설명.md` | 덱 사용법 (F + 이동이 전부) |
| `handoff-2026-07-28.md` | 이 문서 |
| 루트 `README.md` | app-v2 기준 전면 개정 확정 (07-28 실측 반영) |
| `docs/` 3부작 | 읽기 안내·링크 보강 확정 + **하루의 해부에 수량 산식 명시** |
| `slides-content.md` | 머리말에 07-28 개정·정정 목록 (덱이 문서보다 앞섬) |

## 7. 상태

- **git**: 오늘 커밋 전부 push (`b1a6ddb`…HEAD)
- 워킹트리에 `final-project/codex_추천개선안.md`(출처 불명, 미커밋)가 있다 —
  사용자 확인 대기
- **운영 8020**: PID 64727 · 새 코드 · 정상. 오늘 밤 정규장 1분 방어 가동 예정
- **내일 13:05** 텔레그램 요약은 ✅ 성공으로 올 것(버그 수정됨)
- **PDF**: 안 뽑음 — HTML로 발표, 필요 시에만 마지막 1회 (사용자 보류 중)

## 8. 남은 것 — 다음 세션 계획 (사용자 예고)

1. **PPT 전체 개선 계속** — 17장을 처음부터 다시 훑으며 다듬는다
   (사용자 검토 코멘트 기준은 NEXT.md §4의 "지적 패턴" 참조)
2. **영상 최종 수정** — 마지막 단계로 예약 (재빌드 명령은 §2)
3. **리허설** — 대본.md 들고 소리 내어 1회, 12:30 목표 (아직 0회)
4. 발표 당일: 클론→영상 재생 테스트 · §5-3 숫자 재조회 → 깔때기·결과 장 갱신
5. (선택) PDF 1회 출력 · 발표 후: 알림 중복 억제, SEC 소스 403 처리

±5% 재판단 질문 답(사용자 문의로 정리): 미완성이 아니라 **완성 후 운영
스위치만 꺼 둔 것**(`rejudge.enabled: false`). 데모 각본·통합 테스트로
검증됐고(영상 1:20), 유료 자동 기능이라 "깨끗한 하루 검증 → 켜기" 순서.
켜기 전 수정 항목: SEC 소스 403 처리(open-items §3-4).
