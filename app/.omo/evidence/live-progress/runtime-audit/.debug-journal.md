# Runtime Debug Journal — H3 configuration isolation and paper fail-closed gate

Started: 2026-07-13
Goal: Confirm that tests override operator configuration deterministically and incomplete paper-trading configuration fails closed, without reading `.env`.

## Environment snapshot

- Runtime: Python 3.11.15 through `uv run --project`.
- Working directory for probes: a fresh `mktemp -d` directory, so the repository `.env` is not discoverable by `Settings`.
- Network / database: no network, Docker, PostgreSQL, or localhost:5432 access used.
- References read: debugging Python runtime, setup, investigation, QA, cleanup, partial-runtime-evidence.

## Hypotheses

1. [INVESTIGATING] The pytest test-runtime environment overrides conflicting provider/database values, producing mock/fixture/memory/no-trading settings. Distinguishing evidence: a controlled subprocess observes those five fields after applying the test gate.
2. [INVESTIGATING] Selecting Alpaca or enabling trading without all required credentials/control token raises a validation error before runtime composition. Distinguishing evidence: controlled `Settings` construction rejects each incomplete combination.
3. [INVESTIGATING] A paper-enabled request without the control-room token is rejected by the live FastAPI surface. Distinguishing evidence: `TestClient` observes HTTP 403; a same-origin valid-token request reaches the normal validation path.

## Artifacts to revert

- [x] `mktemp -d` probe directory — removed by shell trap after each probe.
- [ ] This journal and the final audit verdict are retained as requested evidence; no source or test files will be modified.

## Findings

- First controlled probe initially retained its own placeholder Alpaca environment variables while testing a missing-key case; this invalidated that specific assertion. The temporary directory was removed. The follow-up will delete the controlled key variables before each negative case.
- Follow-up controlled probe: `CONTROLLED_SETTINGS_AND_ROUTE_AUDIT_PASS`.
  - After applying the exact test-runtime gate values, a `Settings` subclass with `env_file=None` observed `broker=mock`, `data=fixture`, `database=memory`, `llm=mock`, and `trading_enabled=False`, despite initially conflicting controlled values.
  - With the controlled credential/token variables deleted, construction rejected: Alpaca without an API key (`alpaca_api_key`); trading without Alpaca mode (`broker_mode=alpaca`); and enabled Alpaca with credentials but no control-room token (`control_room_token`).
  - A valid controlled paper configuration produced HTTP 403 for a tokenless `POST /api/runs`; a same-origin form request with the correct controlled token reached normal ticker validation and returned the dashboard’s 200 response.
- Focused regression run from the temporary no-`.env` directory: `22 passed in 1.19s` for `tests/unit/test_config.py` and `tests/unit/test_control_room_access.py`.

## Cleanup

- [x] Both temporary working directories were removed by shell traps.
- [x] No process, port, Docker resource, PostgreSQL resource, source file, test file, or operator environment file was changed.
