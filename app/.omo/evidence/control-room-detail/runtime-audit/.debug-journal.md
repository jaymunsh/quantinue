# Runtime Debug Journal — H3 Test Isolation

Started: 2026-07-13 (Asia/Seoul)
Goal: Verify that the test entry points force deterministic safe modes independently of an active local `.env`, and that the explicit real-key opt-in remains available without making a provider call.

## Environment snapshot (Phase 0)

- Runtime: Python 3.11.15 via `uv run`.
- Entry points: `uv run pytest -q`; `sh scripts/test_postgres_integration.sh -q`.
- PostgreSQL rule: do not inspect, use, or alter localhost:5432. The integration script selects a disposable loopback port in 55400–55499 and removes its named container via its EXIT trap.
- Git HEAD: `ba0c742`; worktree was already dirty/untracked before this audit.
- `.env`: deliberately not opened or printed.
- References read: debugging SKILL.md; runtimes/python.md; methodology/00-setup.md; 02-investigate.md; 08-qa.md; 09-cleanup.md.

## Hypotheses

1. [PENDING] `tests/conftest.py` overwrites test-mode environment values before test imports, so bare pytest remains safe and deterministic even if `.env` selects local LLM/public data. Distinguishing evidence: bare suite passes without remote-provider activity.
2. [PENDING] `scripts/test_postgres_integration.sh` exports its own mock/fixture settings and a runner-created test database URL, so PostgreSQL integration does not inherit active application LLM/data settings. Distinguishing evidence: the disposable-runner suite passes and its cleanup removes the named runner container.
3. [PENDING] `QUANTINUE_RUN_REAL_KEY_TESTS=1` disables only the default real-key skip gate; clearing the OpenAI key makes the real-key test skip at its own preflight rather than contact a provider. Distinguishing evidence: explicit-opt-in run reports the test's missing-key skip reason, not the default safety-gate reason.

## Failed hypothesis round counter

- No failed round at journal creation.

## Artifacts to revert

- [ ] Disposable PostgreSQL container created only by `scripts/test_postgres_integration.sh`; its EXIT trap removes it. Verify no `quantinue-test-*` runner remains.
- [ ] This durable audit journal and verdict are requested evidence, retained under `.omo/evidence/control-room-detail/runtime-audit/`.

## Findings

### Investigation round 1 — 2026-07-13
- `TestClient` observed HTTP 200 for terminal-detail API, control-room API, and dashboard.
- Credential-bearing reference: API `href` was null and the displayed label was the credential-free origin/path.
- Control-character reference: API `href` was null and the displayed label was `invalid reference`.
- The synthetic marker was absent from all three response bodies.
- The first assertion run was intentionally stopped as **inconclusive for anchor attributes**: its fixture included no approved HTTP(S) reference, so it could not prove the dashboard anchor guard branch. This is a fixture-coverage issue, not a product failure; next run adds an approved reference plus each hostile case.

### Manual runtime QA — 2026-07-13
- Scenario: two isolated memory/mock FastAPI applications, one with a credential-bearing HTTP URL and one with an ASCII-control-character URL in terminal detail. Each application also carried an approved HTTPS reference to exercise the allowed-link branch.
- Command: `uv run python - <<'PY' ... TestClient(create_app(store=AuditStore(run))) ... PY`
- No Docker, PostgreSQL, `.env`, external network, or localhost:5432 was accessed.
- Observed for both cases:
  - `GET /api/runs/{id}/detail`, `GET /api/runs/{id}`, and `GET /` each returned HTTP 200.
  - synthetic sensitive marker absent from every response body;
  - hostile reference `href` was null;
  - credential-bearing input displayed only credential-free origin/path; control-character input displayed `invalid reference`;
  - hostile raw reference was absent from dashboard HTML;
  - approved HTTPS reference preserved its API `href` and rendered with `target="_blank"` plus `rel="noopener noreferrer"`.
- Silent-failure check: no 2xx/default response hid a failed assertion; all requested status and semantic assertions completed successfully.
- Regression confirmation: `uv run pytest -q tests/unit/test_api_terminal_detail.py tests/test_dashboard_detail.py` → `17 passed in 1.47s`.

## Cleanup
- [x] One-shot Python processes exited; no listener, container, database, or persisted run was created.
- [x] No temporary script, screenshot, trace, debugger, environment override, or source edit was created.
- [x] Scoped evidence journal retained intentionally; no synthetic marker or credential value is recorded in evidence.

Pending runtime commands.
