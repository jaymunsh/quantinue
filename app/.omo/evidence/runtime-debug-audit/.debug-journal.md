# Runtime debug audit journal

## Scope and isolation

- Audit date: 2026-07-13
- Purpose: independently test G2 hypotheses H1–H3 against the current Quantinue MVP.
- Safety boundary: do not inspect, use, stop, or change any host `localhost:5432` container, volume, or network. PostgreSQL evidence may use only `scripts/test_postgres_integration.sh` and its disposable high-port runner. Product Compose may use host `5444` and internal `db:5432` only.
- Sensitive data: this journal records no credentials, connection URLs, container identifiers, or port numbers selected by the disposable runner.

## Hypotheses

- H1: A conflicting replay overwrites append-only ledger provenance, model, or raw values.
- H2: A strategist `sell` decision can cross either the model contract or real PostgreSQL boundary.
- H3: The stale-classifier cleanup regressed transient-retry or terminal-failure classification.

## Artifact register

- Created this journal before runtime commands.
- Planned temporary runtime resources: the runner-owned disposable PostgreSQL container only; cleanup is its shell trap.
- Planned evidence output: `verdict.md` in this directory, redacted to exclude secrets, URLs, container names, and dynamically selected ports.

## Findings

### 2026-07-13 — H1 append-only conflicting replay

- Source: `sh scripts/test_postgres_integration.sh -q`
- Observed value: `19 passed in 9.48s`
- Runtime path: disposable PostgreSQL runner, including `tests/integration/test_append_only_postgres.py::test_postgres_keeps_first_ledger_rows_when_conflicting_payload_replays`.
- Interpretation: the real database conflict/replay boundary preserved the original raw and provenance/model fields asserted by that adversarial test; H1 is refuted.
- Refutes/Confirms: H1 refuted.

### 2026-07-13 — H2 strategist sell model and PostgreSQL boundary

- Source: `sh scripts/test_postgres_integration.sh -q`
- Observed value: `19 passed in 9.48s`
- Runtime path: disposable PostgreSQL runner, including `tests/integration/test_domain_lifecycle_postgres.py::test_postgres_rejects_phase_two_sell_strategist_signal`; model rejection is also covered by the current unit suite in the final gate.
- Interpretation: the real schema rejected the phase-two strategist sell insertion, so a sell cannot pass the PostgreSQL boundary; H2 is refuted.
- Refutes/Confirms: H2 refuted.

### 2026-07-13 — H3 retry and terminal classifier behavior

- Source: `PYTHONASYNCIODEBUG=1 uv run pytest -q tests/unit/test_pipeline_resilience.py tests/unit/test_retry.py`
- Observed value: `28 passed in 0.36s`
- Runtime path: minimal in-memory pipeline execution, including transient retrying-to-completed and auth/non-transient HTTP single-attempt cases.
- Interpretation: the shipping orchestrator still persists retrying attempts for transient failures and does not retry terminal auth or non-transient HTTP failures; H3 is refuted.
- Refutes/Confirms: H3 refuted.

### 2026-07-13 — H3 direct classification readout

- Source: `uv run python -` minimal runtime driver
- Observed value: `TransientFailureError True failed TRANSIENT_FAILURE temporary provider failure`; `HttpFailureError False failed HTTP_FAILURE upstream HTTP failure`; `AuthenticationFailureError False failed AUTHENTICATION_FAILURE provider authentication failed`
- Interpretation: the active classifier exposes the expected transient versus terminal distinction with redacted stable persistence messages.
- Refutes/Confirms: H3 refuted.

## Cleanup

- The disposable runner exited successfully and owns cleanup through its scoped exit trap; no runner identifier, port, URL, or credential is retained in this evidence.
- No debug source instrumentation, temporary scripts, environment overrides, listeners, or browser artifacts were created by this audit.
- The pre-existing dirty/untracked worktree remains preserved. This audit added only this evidence directory and the redacted ledger event.
